extends ../layouts/application.pug

block header
  = t('views.tasks.new.create')

block content
  +formFor()(action=route('tasks') method='post')
    +input(task, 'name', errors)(type="text")
    +textarea(task, 'description', errors)
    +select(task, statuses, '', 'statusId', 'status', errors)
    +select(task, users, '', 'executorId', 'executor', errors)
    +select(task, tags, tasksTags, 'labels', 'tag', errors)(multiple='multiple')
    +submit(task)(value=t('views.tasks.new.send'))
