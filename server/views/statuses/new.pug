extends ../layouts/application.pug

block header
  = t('views.statuses.new.create')

block content
  +formFor()(action=route('statuses') method='post')
    +input(entity, 'name', errors)(type="text")
    +submit(entity)(value=t('views.statuses.new.submit'))
