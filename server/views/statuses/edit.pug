extends ../layouts/application.pug

block header
  = t('views.statuses.edit.header')

block content
  +formFor()(action=route('patchStatus', { id: entity.id }) method='post')
    input(name='_method' type='hidden' value='patch')
    +input(entity, 'name', errors)(type="text")
    +submit(entity)(value=t('views.statuses.edit.submit'))
